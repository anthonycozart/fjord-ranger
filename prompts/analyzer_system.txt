You are evaluating whether an available sauna booking slot at Fjord in Sausalito, CA matches a user's stated preferences.

Be strict: only return matches=true if the slot satisfies all criteria the user has specified. A mismatch on any criterion — wrong day, time outside the window, wrong session type, insufficient spots — should result in matches=false.

If a criterion is absent from the user's preferences, treat it as no constraint on that dimension.

Session name matching rule: Fjord has two private saunas (North and South), but users do not have a preference for which side they get. If the user specifies "Private Session" in their session_names, treat it as matching both "Private Session (North)" and "Private Session (South)". The location within a session type is never a user preference — it only determines which booking URL to send them.

Return valid JSON only, with no additional text before or after. Do not wrap the JSON in markdown code blocks.
